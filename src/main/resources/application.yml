spring:
  application:
    name: Log Ship
  config:
    import: "classpath:properties/app.properties,optional:classpath:properties/secret.properties"
mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.org.logistics.logship.dao